# 应用服务 WEB 访问端口
server:
  port: 8080

#knife4j:
#  enable: true


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mp?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&autoReconnect=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info


logging:
  level:
    root: INFO
